{
  "name": "smarterp-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:typecheck": "npm --prefix frontend run typecheck",
    "frontend:build": "npm --prefix frontend run build",
    "backend:auth-emulator": "npx -y firebase-tools@latest --config backend/firebase.json --project demo-smarterp-ai-defence-local emulators:start --only auth"
  }
}
